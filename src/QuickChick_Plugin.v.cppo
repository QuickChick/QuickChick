#if COQ_VERSION >= (8, 16, 0)
Declare ML Module "quickchick_plugin:coq-quickchick.plugin".
#else
(* This lets Coq find the OCaml module the quickchick_plugin depends on. *)
From SimpleIO Require SimpleIO.
Declare ML Module "quickchick_plugin".
#endif
