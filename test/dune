(coq.theory
  (name QuickChick.Testing)
  (theories QuickChick)
  (modules plugin))

(coq.extraction
  (prelude mutation)
  (extracted_modules mutation))

(test
  (name mutation)
  (modules mutation)
  (flags :standard -w -39-67)
  (libraries zarith))
